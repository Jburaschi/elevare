# Elevare Full (Frontend Flutter)

Ejecutar:
```bash
flutter pub get
flutter run -d chrome --dart-define=BASE_URL=http://localhost:3000
```
Si no hay backend corriendo, la app usa **datos de ejemplo** para que no quede en blanco.
